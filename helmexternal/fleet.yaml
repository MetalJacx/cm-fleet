defaultNamespace: cmtest
helm:
  releaseName: cert-manager
  chart: git@github.com:cert-manager/cert-manager//deploy/charts/cert-manager/Chart.template.yaml
  #repo: https://charts.jetstack.io
  #version: v1.15.3
  values:
    namespace: cmtest
    crds:
      enabled: true
      keep: false
