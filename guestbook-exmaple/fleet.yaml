defaultNamespace: example-gb-joeb
targetCustomizations:
- name: dev
  helm:
    values:
      replication: 2
  clusterSelector:
    matchLabels:
      env: dev

- name: tool
  helm:
    values:
      replicas: 3
  clusterSelector:
    matchLabels:
      env: tools
